image: gcc

stages:
    - build
    - run

build:linux:
    stage: build
    
    before_script:
    - apt update && apt -y install cmake

    script:
        - mkdir build && cd build/
        - cmake ..
        - cmake --build .
        - ./myproject
        - echo ${pwd}

